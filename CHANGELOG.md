# Changelog

## 0.2.0

- Move npm distribution to the repository-owned `@rogerchappel/skillforge` package while retaining the `skillforge` executable.
- Align release metadata and installed-package verification with npm publishing.
- Include the CLI validation, deterministic rendering, and packaging improvements added since 0.1.0.

## 0.1.0

- Initial local-first CLI.
- Canonical manifest and tiny YAML codec.
- Lint, activation fixture tests, render adapters, package command, and report command.
- OpenClaw and Claude plugin-style render targets.
