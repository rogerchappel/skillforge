{
  "name": "skillforge-mvp",
  "requiredChecks": ["npm run check", "npm test", "npm run build", "npm run smoke", "bash scripts/validate.sh"],
  "commands": ["init", "lint", "test", "render", "package", "report"],
  "renderTargets": ["openclaw", "claude-plugin"],
  "artifactPolicy": "local-first; package creates a gzipped review artifact and does not publish",
  "externalWrites": "ask-first except explicit GitHub repository creation requested by operator"
}
